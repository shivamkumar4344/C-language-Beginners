#include<stdio.h>
main()
{
	float p,r,t,s;
	printf("Enter the principal,rate and time:-");
	scanf("%f %f %f",&p,&r,&t);
	s = (p*r*t)/100;
	printf("Simple interst is :-%f",s);
	 
}
//write a progrmm to enter the student details like name,reg_no,roll_no,cgpa,grades
